# Sephiria ModMaker — 更新记录

## 2.5.31 — 2026-09-21

- 为 ModMaker 界面加入了**深色主题**。可以从 `工具 ▾ → 深色界面` 直接切换，或在 `设置 → 界面主题` 中选择跟随系统／浅色／深色。默认跟随系统，更改后无需重启即可立即生效。
- 在 `设置 → ModMaker Runtime` 和 `工具 ▾` 中加入了**重启游戏**。游戏只在启动时读取模组，所以在 `安装到游戏` 之后按下它，就能立刻确认修改内容。进行中的一轮会从游戏保存的位置继续。
- 修复了触发效果**强化物品**填入负数时完全没有反应的问题。现在附魔次数可以降到0以下，物品等级会真正下降；等级低于0时该神器会被关闭。无法强化时，原因（最大等级为0・已达上限）会写入 `Player.log`。
- **服装补丁**现在可以去掉游戏角色的能力值。勾选 `用下面的列表替换原始能力值` 并清空列表，角色就没有任何能力值；也可以按 `复制原始能力值` 填入原值后，只删除想去掉的那一行。数值设为 0 只会关闭效果，该行仍会显示在角色选择画面，因此要彻底去掉请删除该行本身（例如学者蜥蜴的 `魔力环流`）。
- 加入了神器选项**绝对唯一**。整个队伍中只能存在一个；只要有人携带，它就会从所有人的奖励、商店、神秘之壶、奇迹、口袋次元与炼金候选中移除。提示中的 `[唯一效果]` 会变为 `[绝对唯一]`。
- 当游戏中安装了 **DungreedEnemies** 时，也可以使用它的怪物与资源。素材库中会出现 `[Dungreed]` 图像与动作，武器攻击音效的选择中会出现其音效，敌人修改・敌人添加列表中会出现121种怪物（18种Boss）。未安装时不会增加任何内容。

更新编辑器后，请用**安装到游戏**将附带运行时也更新至2.5.31。此版本保存的模组需要运行时2.5.31或更高版本。

## 2.5.29 — 2026-09-18

- 修复了触发效果**强化物品**只增加附魔次数、实际等级却没有提升的问题。像通过 `首次获得时触发一次` 强化物品自身这样看起来没有触发强化的情况均属此类。
- 修复了可以通过游戏的**预设**（应用、粘贴、在预设窗口中编辑列表）带入勾选了 `禁用许愿喷泉` 的物品的问题。这些物品不再出现在预设窗口的列表编辑中，即使已保存的预设里包含它们，开局时也不会发放。

更新编辑器后，请用**安装到游戏**将附带运行时也更新至2.5.29。此版本保存的模组需要运行时2.5.29或更高版本。

## 2.5.28 — 2026-09-18

- 游戏**只按开/关读取的属性**共23种（`ABSOLUTE_EVASION`、`MP_SHIELD`、`INFINITY_MP`、`INFINITY_DASH` 等）现在只接受 **0（关）和 1（开）**。游戏不会看这些属性的数值大小，填2和填5都一样是开启。`输入单位` 栏会显示 `开 1 · 关 0`，各等级数值仍可写成 `0,0,1` 来表示**从某个等级开始开启**。
- 若旧的 `mod.json` 中这类属性填了2以上，游戏读取时会将其修正为开（1），并在 `Player.log` 中记录是哪个模组的哪个属性。游戏只要大于0就一律开启，因此实际效果不会改变。
- 移除了 `EVASION_DISABLE`、`GOLD_HAND` 输入单位上错误标注的 `%`。

## 2.5.27 — 2026-09-18

- 带使用次数限制的物品说明现在显示**实际剩余次数**。由 `首次获得` 生成的 `(1次)` 在使用后会变成 `(0次)`，并在背包、地面和出售列表中按物品分别计数。已打开的说明也会立即刷新。

更新编辑器后，请用**安装到游戏**将附带运行时也更新至2.5.28。此版本保存的模组需要运行时2.5.28或更高版本。

## 2.5.26 — 2026-09-18

- 新增**模组专属功能 → 原版禁用**。在左侧列表 `模组信息` 正下方勾选原版物品、武器、服装、奇迹、天赋后，它们的数据仍留在游戏中，但玩家将无法遇到。它们会从奖励、商店、选项、图鉴和选择界面中移除，已拥有的物品和存档不会损坏。
- 触发效果**强化物品**现在强化的是物品本身而不是格子。会对所选物品附魔数值指定的次数（负数则取消附魔），移动物品时强化也会跟着走。与游戏的附魔一样，只对神器生效，最多到最大等级。
- `仅向使用相同模组的玩家显示房间` 现在始终开启。移除了 `ModMaker 信息` 窗口中的设置行，也不再读取 `config.json` 中的 `lockMultiplayer`。
- 修复了原版图鉴的 `用所选物品创建补丁` 会跳到物品补丁以外标签页的问题。

更新编辑器后，请用**安装到游戏**将附带运行时也更新至2.5.26。此版本保存的模组需要运行时2.5.26或更高版本。

## 2.5.25 — 2026-09-17

- **玩家大小**与**远程弹丸大小**属性现在不仅对神器生效，在奇迹、连携、天赋、增益、武器、效果、队伍共享、原版神器补丁等所有可赋予属性的地方都会生效。属性增幅也与其他属性一样生效。
- 检查了远程弹丸大小是否完整作用于武器子弹、魔法和神器发射的弹丸。大小实际生效时，`Player.log` 中会留下 `크기 스탯 적용`（大小属性生效）一行，便于确认。
- 修复了玩家大小变化瞬间角色朝向可能翻转的问题。

更新编辑器后，请用**安装到游戏**将附带运行时也更新至2.5.25。此版本保存的模组需要运行时2.5.25或更高版本。

## 2.5.24 — 2026-09-17

- 重新设计了**资源库**窗口。默认以缩略图网格显示，也可切换为列表视图。新增各类型结果数量、排序、缩略图尺寸、缩放与背景选择，以及同一对象其他动作的快捷入口。条目很多时滚动也不会卡顿。
- 新增**保存备份与自动保存**。每次保存都会把之前的 `mod.json` 保留在模组文件夹的 `.modmaker-backups` 中（默认10份）；首次保存到已有模组的文件夹时会询问是否覆盖。自动保存可在设置中开关并调整间隔（默认10分钟）。可从工具菜单的**保存备份**打开备份。
- 修复了用属性转换固定属性伤害或转移到其他属性时，与**最高属性伤害**加成相互影响、数值不断上下波动的问题。
- 修复了带有**首次获得时**效果的神器在效果触发后仍显示为未使用的问题。
- 药水无持续时间提升的属性现以与原版药水相同的方式生效。修复了读档继续后消失或残留到下一局的问题。

更新编辑器后，请用**安装到游戏**将附带运行时也更新至2.5.24。此版本保存的模组需要运行时2.5.24或更高版本。

## 2.5.23 — 2026-09-17

- 可逐行选择与队友共享的常驻属性。共享的属性行会在游戏提示中显示**属性共享**标记。
- 属性转换与占用现支持最大HP、最大MP等数值，发动限制和伤害公式也可读取这些值。修复了防御力、暴击伤害、属性增幅等部分属性被读取为0的问题。
- 修正了有使用次数限制的神器在丢弃或出售后的次数显示。
- 修复了失去物品时，随物品一起移除的增益未触发**增益消失时**效果的问题。普通和主动型神器均适用。

更新编辑器后，请用**安装到游戏**将附带运行时也更新至2.5.23。此版本保存的模组需要运行时2.5.23或更高版本。

## 2.5.22 — 2026-09-16

- 在原版图鉴中新增**资源库**。无需启动游戏，即可读取已安装游戏的图片、逐帧动画和原始动画数据，并进行搜索、预览和导入。
- 图片和动作的浏览按钮也会打开资源库。导入时保留图片基准点、游戏内尺寸，以及动画帧率和循环信息。
- 可搜索、试听音效和BGM，并将其选为武器攻击音效。已附带提取工具，无需另行安装Python。

## 2.5.21 — 2026-09-16

- 更新了发布文件。


## 2.5.20 — 2026-09-16

- 修复了**主动型神器上编写的触发效果完全不生效**的问题。编辑器允许编写，但游戏中这些效果被整个忽略。现在主动型神器与普通神器一样能正常触发。
- 更新了发布文件。


## 2.5.19 — 2026-09-16

- 修复了 2.5.18 中 ModMaker 提供的属性（商人商品数、商人传说商品数、商人持有金、药水饮用速度、迷你首领奖励骰子，以及原有的尺寸属性）在游戏内**只显示为“(未知属性)”**的问题。原因是重新注册属性的顺序有误。
- 更新了发布文件。


## 2.5.18 — 2026-09-16

- 修复了物品提示框显示**其他神器说明**的问题。只要常驻属性栏里有一个游戏不认识的名称，提示框就完全无法刷新；现在只会静默跳过那一行。
- **商人商品数、商人传说商品数、商人持有金、药水饮用速度、迷你首领奖励骰子**现由 ModMaker 直接支持，无需其他附加组件也能实际生效。
- 这些属性自带名称与说明，并会在物品提示框中**自动生成说明文字**（例如“新场所的商人多陈列 2 件神器”），无需自己撰写效果文本。
- 其他附加组件注册的属性将从属性列表中隐藏，因为对没有该组件的玩家不产生任何效果。已经使用这些属性制作的模组仍可正常保存。（运行游戏一次以重新获取参考数据后生效）
- 列表中名称的颜色和加粗标签现在按**实际外观**显示，而不是把标签当作文字打印出来。
- 更新了发布文件。


## 2.5.17 — 2026-09-16

- README和更新记录可在GitHub仓库查看，不包含在Release ZIP中。
- 每次启动编辑器都会检查更新。发现新版本后会打开更新窗口；关闭后仍可通过顶部提示稍后更新。关闭自动检查或跳过该版本后不会自动弹窗。

- 更新了发布文件。


## 2.5.16 — 2026-09-16

- 更新了发布文件。


## 2.5.15 — 2026-09-16

- 更新了发布文件。


## 2.5.14 — 2026-09-16

- 物品效果说明可以填写各等级的数值并预览。
- 更新了发布文件。


## 2.5.13 — 2026-09-15

- 整理了随附的说明文档。

## 2.5.12 — 2026-09-15

- 发布版本的更新记录可在 `patchlog` 文件中查看。

## 2.5.11 — 2026-09-15

- 编辑器启动时会检查新版本，点击立即更新后下载并重启。
- 可在工具→检查更新中手动检查，也可关闭自动检查或跳过特定版本。
- 检查更新失败时不再提示已是最新版本。
- 更新保留制作中的模组和设置，替换文件失败时恢复原文件。
